worker: echo "$STITCH_CONFIG" > persist.json && \
        echo "$TAP_CONFIG" > config.json && \
        echo "$CATALOG" > catalog.json && \
        tap-platformpurple -c config.json --catalog catalog.json
